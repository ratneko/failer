version: '3'
services:
  app:
    build: .
    command: bin/rails s
    volumes:
      - .:/app:cached
      - /app/vendor
    depends_on:
      - db
    tty: true
    stdin_open: true

  db:
    image: mysql:5.7
    volumes:
      - mysql-data:/var/lib/mysql

volumes:
  mysql-data:
